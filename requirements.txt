# requirements.txt — Reef Imagery Pipeline (CPU-only)
#
# Install PyTorch CPU build explicitly BEFORE other packages:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
# Then install everything else:
#   pip install -r requirements.txt

# ── Core pipeline ─────────────────────────────────────────────────────────────
numpy>=1.24.0
rasterio>=1.3.0
matplotlib>=3.7.0
pandas>=2.0.0
pyproj>=3.6.0
scikit-image>=0.21.0
requests>=2.31.0
pystac-client>=0.7.0
planetary-computer>=1.0.0
shapely>=2.0.0
python-dotenv>=1.0.0
flask>=2.3.0
opencv-python-headless>=4.8.0   # headless — no GUI deps, smaller, CI-safe
tqdm>=4.65.0
scikit-learn==1.8.0   # pinned: bvi_model.pkl / visibility_rf_bathy.pkl / feature_ranker_model.pkl trained on 1.8.0; other versions silently mis-load (InconsistentVersionWarning)
scipy>=1.11.0
geopandas>=0.13.0
rioxarray>=0.13.0
xarray>=2023.12.0
rasterstats>=0.17.0
netCDF4>=1.6
pyyaml>=6.0                     # config_cpu.yaml loader

# ── Deep learning (CPU build — NO CUDA) ──────────────────────────────────────
# Install with: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# Do NOT install from PyPI directly — that pulls CUDA wheels (3+ GB).
torch>=2.0.0                    # CPU build assumed (see above)
torchvision>=0.15.0             # CPU build assumed
segmentation-models-pytorch>=0.3.3
albumentations>=1.3.0

# ── Optional: lightweight MobileNet-V3 encoder ───────────────────────────────
# timm enables 'timm-mobilenetv3_small_100' encoder in SMP (<1.5 M params).
# Without timm the code falls back to mobilenet_v2 (~2.2 M params, still fast).
timm>=0.9.0

# ── Validation ────────────────────────────────────────────────────────────────
folium>=0.14.0                  # interactive HTML validation map

# ── Benchmarking ─────────────────────────────────────────────────────────────
psutil>=5.9.0                   # cross-platform RSS memory measurement

# ── Optional: CoastSat preprocessing (Task #2) ───────────────────────────────
# Requires Google Earth Engine credentials: earthengine authenticate
# coastsat>=1.1.0               # uncomment when GEE access is available

# ── Optional: CMEMS live Kd490 ────────────────────────────────────────────────
# copernicusmarine>=1.0.0       # heavy; install only when live Kd490 is needed

# ── Dev / test ────────────────────────────────────────────────────────────────
# pytest>=7.4.0
# pytest-cov>=4.1.0
# black>=23.0.0
# ruff>=0.1.0
# mypy>=1.6.0
